import 'dart:ui';

class I18nConfig {
  const I18nConfig();

  static const defaultLocale = Locale('en');

  static const supportedLocales = [
    Locale('en'),
    Locale('es'),
    Locale('fr'),
  ];
}
